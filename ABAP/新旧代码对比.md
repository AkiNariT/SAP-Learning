<details>
<summary><h2>CORRESPONDING #( )：同名字段自动赋值</h2></summary>
  old:<dr/>

  ```abap  
ls_alv-vbeln = ls_vbak-vbeln.
ls_alv-auart = ls_vbak-auart.
ls_alv-kunnr = ls_vbak-kunnr.
ls_alv-erdat = ls_vbak-erdat.

  ```
new:<dr/>
  ```abap
ls_alv = CORRESPONDING #( ls_vbak ).
  ```
</details>

<details>
<summary><h2>String Template：字符串模板</h2></summary>
  old:<dr/>

  ```abap
CONCATENATE '受注番号:' lv_vbeln '得意先:' lv_kunnr INTO lv_msg SEPARATED BY space.

  ```
new:<dr/>
  ```abap
DATA(lv_msg) = |受注番号: { lv_vbeln } 得意先: { lv_kunnr }|.
  ```
</details>
